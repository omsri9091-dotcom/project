[build]
builder = "NIXPACKS"

[env]
PYTHONUNBUFFERED = "1"

[deploy]
startCommand = "python train_model.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
